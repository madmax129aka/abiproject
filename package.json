{
  "name": "skillswap",
  "version": "1.0.0",
  "description": "Skill Swap - Peer-to-Peer Skill Exchange Platform",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && mvn spring-boot:run",
    "install:client": "cd client && npm install",
    "build:server": "cd server && mvn clean package -DskipTests"
  }
}
